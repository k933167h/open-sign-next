services:
  minio:
    image: minio/minio:latest
    ports: [ "9000:9000", "9001:9001" ]
    environment:
      MINIO_ROOT_USER: opensign
      MINIO_ROOT_PASSWORD: opensign_password
    command: server /data --console-address ":9001"

  redis:
    image: redis:7-alpine
    ports: [ "6379:6379" ]
